## 1. Scaffold the skill

- [ ] 1.1 Create `.agents/skills/social-image/` with `SKILL.md` (frontmatter: name, description with trigger examples) and a `scripts/` dir.
- [ ] 1.2 Add the `.claude/skills/social-image` symlink → `../../.agents/skills/social-image`, and a `skills-lock.json` entry. Do NOT touch other agent dirs / do NOT install globally.

## 2. Renderer script

- [ ] 2.1 Add `scripts/social-image.mjs` — a Node ESM CLI using Playwright (headless Chromium), fixed viewport 1200×630, `deviceScaleFactor: 1`.
- [ ] 2.2 Mode A (`--url <landing> [--selector <css>]`): load the page, `waitForLoadState('networkidle')` + `document.fonts.ready`, screenshot the hero element (default selector list, overridable), clipped to 1200×630. Fail loudly if the selector matches nothing.
- [ ] 2.3 Mode B (`--hero --title --tagline --icon <path>`): render `scripts/hero-template.html` from a temp dir with values filled and ui-kit `tokens.css` + `ga-ui-kit.css` loaded; screenshot the full viewport. Never write the hero into the project.
- [ ] 2.4 `--out public/og-image.png` (default); ensure the output is a 1200×630 PNG.
- [ ] 2.5 Handle Chromium install (`npx playwright install chromium`) with a clear message when missing.

## 3. Throwaway hero template

- [ ] 3.1 Add `scripts/hero-template.html`: a 1200×630 hero (project name, tagline, icon) styled with GA ui-kit tokens/components, dark house theme, with placeholder tokens the script substitutes.

## 4. SKILL.md content

- [ ] 4.1 Document: what it produces (1200×630 OG image), the two modes, prerequisites (Node, Playwright/Chromium), example invocations, and the OG-tag snippet to paste into the project (`og:image` + `twitter:image` absolute URL).
- [ ] 4.2 State explicitly that it uses **no AI** and that the throwaway hero is **never deployed**.

## 5. Update pet-project-metadata

- [ ] 5.1 Add a "Social image" row to the required-metadata table (1200×630 `og-image.png` generated by `social-image`, referenced via `og:image`).
- [ ] 5.2 Add an audit-checklist item and a conventions bullet; cross-link the `social-image` skill.

## 6. Verify

- [ ] 6.1 Run mode B against a sample project (name/tagline/icon) and confirm a valid 1200×630 PNG is produced.
- [ ] 6.2 Run mode A against an existing landing (e.g. stereoscope or site-review) and confirm the hero is captured at 1200×630.
- [ ] 6.3 Confirm the symlink resolves, `skills-lock.json` is valid, and no stray agent directories were created (`git status` clean except intended paths).
