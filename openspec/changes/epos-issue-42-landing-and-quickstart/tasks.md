## 1. Verify the two claims the whole change rests on

Sequenced first because both were read from the code and both would invalidate
later tasks if wrong. Cheap to check, expensive to discover late.

- [ ] 1.1 Confirm `--set x=false` leaves the feature **on**: build any skill with a `{{ if .Values.x }}` guard, install with `--set x=false`, and observe the guarded section present. Then confirm `--set x=` and a YAML `x: false` both leave it absent. This is the defect tracked as **epos#47**; #42 documents around it, does not fix it, and design D4 lists what to delete here once #47 lands.
- [ ] 1.2 Confirm a git `FROM` against `gaarutyunov/workspace` resolves a skill subdirectory: `FROM git+https://github.com/gaarutyunov/workspace#<ref>:.agents/skills/go-project-scaffold` builds. If the repository is private to the token in play, this is where it surfaces — and it is the D1 assumption that lets #42 proceed without #44.
- [ ] 1.3 Confirm the stage-scope hazard is real: a file brought in by `COPY --from=pro` that contains `{{ .Values.foo }}` must be given `pro.foo`, not top-level `foo`. Note the exact failure message so the quick start can quote it if useful.
- [ ] 1.4 Confirm `REPLACE` compiles an RE2 pattern with `(?s)` so a **multi-line** block can be matched. Task 2.3's `Result[T]` strip sits inside a section whose remainder is kept, so a section-boundary `AWK` would take `Pair` with it (design D6b). If multi-line `REPLACE` is not expressible, say so before writing the Skillfile — the fallback loses `Pair`, and that is a decision, not an implementation detail.

## 2. `examples/go-house/` — the recipe, before anything documents it

Written before the quick start because the page quotes this file. Writing the
page first means writing prose about a build nobody has run.

- [ ] 2.1 `examples/go-house/Skillfile` — **five** stages: `idiomatic` (spf13 `go`), `pro` (`golang-pro`), `cli` (`cobra-viper`), `containers` (`testcontainers-go`), and an unnamed final stage continuing from `go-project-scaffold`. Pin every git source to a tag or SHA (design D5).
- [ ] 2.2 The `idiomatic` stage: strip the "modern stdlib / current syntax" range and the debugging essay from the 774-line `SKILL.md` with `AWK` on section boundaries — **not** `PATCH` (design D5). Land the remainder as `references/idiomatic-go.md`, whose layout section is inside the stripped range (design D6 — this is what keeps the example out of the `internal/` argument).
- [ ] 2.3 The `pro` stage. **`references/generics.md` is kept** — the owner's ruling, and the material is good (design D6b). Precisely: `AWK` out `## Generic Interfaces` (≈270-309, the `Container[T any] interface` + `ProcessContainer` shape `go/SKILL.md:409-413` calls *"this is Java"*); `REPLACE` out the `Result[T]` block inside `## Generic Pairs and Tuples` (≈199-228, from `// Generic Result type (like Rust's Result<T, E>)` through `UnwrapOr`) while **keeping `Pair`/`Swap`** above it — see task 1.4; `REPLACE` `constraints.Ordered` → `cmp.Ordered` (`:9`, `:323`, `:440`) and fix the bare `import "constraints"` (`:40`) — note `constraints.Integer | constraints.Float` (`:44`) has no `cmp` equivalent, so the real `golang.org/x/exp/constraints` path must survive alongside `cmp`. Then `RM references/project-structure.md` whole, and `AWK` out `interfaces.md`'s "Interface Satisfaction Verification" (`:173-181`) and `concurrency.md`'s static worker pool (`:15-51`). Each drop is a named conflict, not a length cut — keep the attributions where a reader of the Skillfile can see them.
- [ ] 2.4 The `cli` stage (`cobra-viper`, 446 lines) — the owner's *"drop viper"* (design D6c). `AWK` out `## Viper Configuration Patterns` (**251-362**, ending cleanly at `## Version Handling` at 363); `REPLACE` the residual mentions the boundary misses — the title at `:12`, `### Unified Configuration` at `:31`, any Viper line under `## Common Mistakes` (438+); `APPEND` the koanf translation from `.claude/rules/go-cli-koanf.md` in its place. Land it as `references/cli.md`. Everything else — command-first architecture, the constructor-per-command factory, `RunE`, context-aware commands, `Args`, flag design, in-memory testing — is kept verbatim. **This is the example's strongest demonstration**: an override the workspace could not layer onto the skill, layered onto the skill.
- [ ] 2.5 The `containers` stage: `COPY --from=containers` only the Go example file(s) the profile needs.
- [ ] 2.6 Final stage: `SET name go-house`, `SET version …` (no source has a top-level `version` for the packer to use), then `AWK` the fixed `## Non-negotiable` block out and `APPEND` a parameterised replacement guarded on `{{ if .Values.openapi }}`, `di`, `telemetry`, `testcontainers`, plus one **string** parameter so both parameter shapes appear. Configuration stays fixed at koanf — it is a standing preference, not a per-project axis, and after 2.4 the derived skill actually says so in its own `references/cli.md`.
- [ ] 2.7 `values-service.yaml` (everything on) and `values-library.yaml` (the single-purpose-library case the scaffold skill names). **YAML booleans**, and supply values for every stage scope the copied files need (task 1.3).
- [ ] 2.8 A `//go:build integration` test that builds the Skillfile with the real builder and installs it under **both** profiles, asserting the guarded sections present under one and absent under the other. Assert the missing-value failure too. Also assert the two drops the owner named: the installed files contain **no** `var _ ` interface assertion and **no** occurrence of the rejected configuration library — an upstream bump that reintroduces either must fail the build, not ship.
- [ ] 2.9 Check the resulting skill reads as **one** skill: one entry document routing to its references, and no two surviving references opposed on the same question. Specifically check the pair this revision creates: the `idiomatic` stage's generics guardrails (`go/SKILL.md:380-429`, inside the kept range) and the `pro` stage's kept `references/generics.md` must agree after the two strips in 2.3. This is the acceptance criterion the issue actually cares about, and no test will catch it.

## 3. `Base.astro` — the layout, before the pages that use it

- [ ] 3.1 **One shell for every page** (design D3, revised — there is no `width` prop; an earlier draft had one and the inner-page measurement killed it). `main` goes from `max-width: 52rem` (832px) to the reference site's **1152px** with `px-4 sm:px-6 lg:px-8` gutters, and the site header becomes sticky at **56px** over the opaque background, no border, no blur.
- [ ] 3.1a The **9/3 grid** for docs pages: a twelve-column grid above the large breakpoint, content in nine columns, `aside` in three, one gap value (the reference site uses two — pick one, design D3). Stacks below the breakpoint. The landing opts out by filling the container, not by changing its width. Measure the content column at the 1152px cap: it must land at **≈824px**, i.e. no wider than today's 832px — if it does not, the grid is wrong, not the requirement.
- [ ] 3.1b The **breadcrumb**, one shared definition, above every docs page's `h1` (design D3a). CV variant: `font-mono`, 14px, `--ga-muted` link → `--ga-fg` on hover, `/` separator with 8px on both sides, current page plain in `--ga-fg`, 24px down to the title. Plus the three things the reference site omits: `aria-label="Breadcrumb"` on the `<nav>`, `aria-current="page"` on the last crumb, `aria-hidden="true"` on the separator. Two crumbs only — `epos / <page title>`. The landing carries none.
- [ ] 3.1c Delete the per-page "back to the landing" link from all four pages — the root crumb replaces it.
- [ ] 3.1d Visible `h1` on the three docs pages: `text-4xl font-semibold tracking-tight` equivalents (36px/40px, weight 600, `-0.025em`), 24px below. The landing's `sr-only` `h1` stays as the one exception.
- [ ] 3.1e Note for the implementer: the generated pages' `<Base …>` call, breadcrumb and aside all live in `internal/docsgen/page.go`. Editing it is **correct and required** here — the generator is extended, never bypassed. Do not hand-edit the `.astro`.
- [ ] 3.2 Global spacing for the kit's block components — `ga-note`, `ga-code`, `ga-card` — from the spacing tokens. This is the single-point fix for "notes are stuck to each other": the component sets `:host { display: block }` and `.note { margin: 0 }`, and no page supplies a margin.
- [ ] 3.3 Token corrections: `--ga-fg-muted` → `--ga-muted` (the former does not exist, so every muted string on the site is currently the `#a1a1a1` fallback), accent fallback `#3b82f6` → `#54a2ff`, and adopt `--ga-font-sans` / `--ga-font-mono` in place of the hand-written system stack.
- [ ] 3.4 Shared furniture: the breadcrumb (3.1b), the footer and the section-label pattern, all currently duplicated across four pages. The section label is the reference site's `SectionLabel` exactly — 14px mono, weight 500, UPPERCASE, tracking **0em** (not widened), foreground-coloured, 14px below, no rule. The footer stays even though the reference site has none: a docs site needs repository and licence links a personal site does not.
- [ ] 3.4a Inner-page list patterns from the reference site: em-dash bullets (`flex gap-3` with a leading `—` nudged `mt-1`/`mt-0.5`, never a real list marker), ruled key/value rows (`flex justify-between border-b py-2`, key in foreground, value in muted mono), pill chips (`rounded-full border`, group gap 6px). Rhythm from `space-y-*`, never element margins.
- [ ] 3.4b **Where the reference site has no answer, say so rather than invent a pedigree.** It loads `@tailwindcss/typography` and never uses `prose`; it has no `h3`, no styled paragraph-in-flow, no inline code, no code block, no blockquote and no table anywhere. epos needs all of those. Derive them from the scale already in use (the `h1`/`SectionLabel`/body sizes above) and record in the page source that they are extensions, not copies.
- [ ] 3.5 Remove `quickstart.astro`'s local `ga-code { margin-bottom: 0.75rem }` so it does not fight 3.2.

## 4. The landing

- [ ] 4.1 ASCII `EPOS` wordmark: `<pre>` with `aria-label`, `--ga-font-display`, negative tracking and tight leading, `select-none`, scaling down at small viewports. #44 wants the same lettering for the catalog, so build it as something reusable rather than inline art.
- [ ] 4.2 The hero band: two-column `[auto 1fr]`, wordmark left, **one** balanced muted sentence right, `sr-only` `<h1>`, stacking below the large breakpoint. **No buttons** (design D2a).
- [ ] 4.3 The install band: mono uppercase section label over `ga-code prompt="$"`, with the quick-start and reference links directly beneath — the two `ga-button`s move up from the page footer.
- [ ] 4.4 Six feature cards: 1/2/3 columns, 16px gap, border-only over a low-alpha fill, colour-only hover. Each card is a capability in the shipped CLI or the shipped instruction set. **Check every card against `SPEC.md` for a withdrawn capability** — no write path, no `push`, no native discovery, no Kubernetes installer, no download statistics.
- [ ] 4.4a The landing fills the shared 1152px container — no aside, no width of its own, no `width` prop (design D3). Its only structural difference from a docs page is the absent 9/3 split.
- [ ] 4.5 Cut the three Skillfile-mechanics sections. Before deleting each, confirm its information is on the quick start or the reference — or that it was a claim the code does not support.
- [ ] 4.6 Confirm nothing above the feature list says "Skillfile", "layer", "manifest" or "digest".

## 5. The quick start

- [ ] 5.1 Rewrite to **≤ 700 words** of prose (from ~2480). Delete the "What you need" preamble outright — a reader who needs *registry*, *digest* and *OCI* defined is not the audience for a quick start.
- [ ] 5.2 Path A, **consuming a published skill, first**: `pull` → `install -f values.yaml` → `ls`. Reachable without writing a `SKILL.md`, and its subject is a skill the reader did not write — which is the coverage gap today (step 5's `pull` currently pulls back what step 4 published).
- [ ] 5.3 Path B, authoring: write, `pack`, publish, `pull` back. Publishing is `oras cp`; do not write an `epos push` that does not exist (#43). Keep it shorter than Path A.
- [ ] 5.4 The worked example section, quoting `examples/go-house/Skillfile` — stages, a cross-stage copy, a whole-file drop, a section edit **and a section replaced by the house's own answer**, plus one sentence on why deriving beats forking. Make that sentence the koanf case (task 2.4): the workspace wrote its Viper override *outside* the skill because an edit inside would be lost on the next reinstall, and the derivation puts it back in. It is the strongest argument on the page and it is true.
- [ ] 5.5 The parameterisation demo: install with `values-library.yaml`, show the sections gone; re-install the **same artifact** with `values-service.yaml`, show them back, and say no rebuild was needed. Show `--set x=` once as the one-off off-switch with the one-sentence reason naming **epos#47**, and **never** `--set x=false` (design D4). Mark that sentence and that form in the source as belonging to #47, so whoever closes it can find and delete them; the values-file profiles stay the primary path either way.
- [ ] 5.6 Retain exactly the prose that prevents a wrong command: `install` resolves from the local store and does not fetch; a value nobody supplied fails the install. At most three notes on the page.
- [ ] 5.7 Audit every command against the cobra tree: no `init`/`new`/`push`/`login`/`template`/`lint`; `--registry` present where required; one argument order for `build` across the whole site; every link through the base-path helper.

## 6. The generated pages — extended, never bypassed

The owner's ruling: *"We should keep auto generated references, this is
important."* Nothing in this section replaces a generated page with a hand-written
one, forks one, or exempts one from the drift gate. Where the generator cannot
express something, the generator gains it.

- [ ] 6.1 Apply the token corrections to the style blocks in `internal/docsgen/cli.go` and `internal/docsgen/skillfile.go`, and the shared furniture in `internal/docsgen/page.go`. **Go, not Astro** — CI regenerates and fails on a diff.
- [ ] 6.1a Extend `internal/docsgen/page.go` to emit the breadcrumb (3.1b) from each page's own title, the visible `h1` (3.1d) and the 9/3 grid wrapper (3.1a). This is the generator gaining a capability, which is the point.
- [ ] 6.1b Fill the two reference pages' asides from what the generator already knows: an on-page contents list built from the sections it emits, plus a link to the sibling reference. `cli.astro` is ~2109 words and `skillfile.astro` ~3341 with no within-page navigation at all — this is where the aside earns its column, and it is derivable, not hand-listed.
- [ ] 6.2 Run `go run ./internal/docsgen`, commit the output, and confirm `go run ./internal/docsgen -check` is clean.
- [ ] 6.3 Confirm the reference pages' **documentary** content is byte-identical — only chrome and style changed. `escape()` turns `{`/`}` into entities for Astro while the `<style>` blocks keep literal braces, so a careless refactor there is silent.

## 7. Before opening the pull request

- [ ] 7.1 `npm run build` in `docs/` clean, and again with the preview base path set — links and assets must resolve under a sub-path.
- [ ] 7.2 Read **all four pages** at 375px, 768px and 1440px. Confirm nothing scrolls horizontally, adjacent notes are separated, the ASCII wordmark fits, the aside stacks below the content on narrow viewports, and the breadcrumb sits 24px above every docs page's title.
- [ ] 7.2a Measure the content column at 1440px: ≈824px, no wider than the 832px it was. If the grid gap or gutters push it wider, fix the grid — the reading measure is the constraint the 1152px shell has to respect.
- [ ] 7.2b Screen-reader pass on one docs page: the breadcrumb announces as a labelled navigation landmark, the current page is marked as current, and the `/` is not read aloud.
- [ ] 7.3 Grep the built output for `#a1a1a1`, `#3b82f6` and `--ga-fg-muted`. All three should be gone.
- [ ] 7.4 Recount the quick start's prose words against the 700 budget. A budget nobody measures is a suggestion.
